{
  "buildCommand": "yarn build",
  "outputDirectory": "public",
  "devCommand": "yarn dev",
  "installCommand": "yarn install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
